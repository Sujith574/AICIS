services:
  - type: web
    name: aicis-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "cd backend && gunicorn -w 4 -k uvicorn.workers.UvicornWorker main:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: MONGO_URI
        value: "mongodb://localhost:27017" # This won't work on Render if no MongoDB, but we use Mock.
      - key: MONGO_DB
        value: "aicis_db"
      - key: MODEL_PATH
        value: "../../ml/model.pkl" # Correct path relative to backend/services/
      - key: SCALER_PATH
        value: "../../ml/scaler.pkl"
      - key: PYTHONUTF8
        value: "1"
